name: luacheck

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
  pull_request:
    branches: [main]
    paths:
      - 'frontend/**'
  workflow_call:

jobs:
  luacheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Lua and luacheck
        run: |
          sudo apt-get update
          sudo apt-get install -y lua5.1 luarocks
          sudo luarocks install luacheck

      - name: Run luacheck
        run: luacheck frontend/

      - name: Test source status screens
        run: lua5.1 frontend/tests/source_status_test.lua

      - name: Test reading direction across chapter changes
        run: lua5.1 frontend/tests/reading_direction_test.lua
